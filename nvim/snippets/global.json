{
  "uuid": {
    "prefix": "uuid",
    "body": [
      "${VIM:system('python3 -c \"import uuid, sys;sys.stdout.write(str(uuid.uuid4()))\"')}"
    ]
  },
  "unixtime": {
    "prefix": "unixtime",
    "body": [
      "${VIM:strftime('%s')}"
    ]
  },
  "datetime": {
    "prefix": "datetime",
    "body": [
      "${VIM:strftime('%Y-%m-%d %H:%M:%S')}"
    ]
  },
  "date": {
    "prefix": "date",
    "body": [
      "${VIM:strftime('%Y-%m-%d')}"
    ]
  }
}
